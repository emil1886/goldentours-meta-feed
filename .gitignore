# Local dev only — not part of the published feed pipeline
.venv/
__pycache__/
*.pyc
out/
probe*.py
inspect_gate.py
validate.py

# Generated square images: regenerated + deployed via the Pages artifact each
# run, so no need to commit them (keeps the repo lean).
docs/images/
